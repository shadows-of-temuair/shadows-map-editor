[workspace]
members = ["archive", "map", "render", "editor"]
resolver = "3"

[workspace.package]
edition = "2024"
version = "0.5.0"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = 2
panic = "abort"
strip = "symbols"
