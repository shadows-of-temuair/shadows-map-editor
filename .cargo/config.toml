# Statically link the MSVC C runtime so Windows builds are standalone
# (no vcruntime140.dll dependency)
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]

[target.aarch64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
